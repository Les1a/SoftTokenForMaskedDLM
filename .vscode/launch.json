{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: use shell to debug",
            "type": "debugpy",
            "request": "attach",
            "connect": {
                "host": "localhost",
                "port": 5678
            }
        }
    ]
}
